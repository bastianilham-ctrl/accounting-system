[pytest]
# Jalankan dari ROOT project: ACCOUNTING_SYSTEM/
# Bukan dari dalam folder tests/
# Contoh: cd ACCOUNTING_SYSTEM && pytest tests/ -v
testpaths = tests
asyncio_mode = auto
log_cli = true
log_cli_level = INFO

# Tambah root project ke sys.path otomatis
pythonpath = .
